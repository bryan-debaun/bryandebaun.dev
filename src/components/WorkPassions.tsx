export default function WorkPassions() {
    // Placeholder component — will be fleshed out with specific work/passions content later
    // Keeping initial markup identical to previous inline content to avoid visual diffs
    return (
        <section>
            <h3>Work & Passions</h3>
            <p>Selected work, passions, and advocacy are surfaced in the sections below.</p>
        </section>
    );
}
