name: A11y & Visual Regression

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  issues: write

jobs:
  run-a11y:
    name: Run A11y & Visual Regression
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Build
        run: npm run build

      - name: Start server
        run: |
          npm run start &
          echo "Waiting for localhost:3000..."
          npx wait-on http://localhost:3000 --timeout 60000

      - name: Run A11y script
        run: npm run a11y:ci

      - name: Run visual snapshot tests
        run: |
          echo "Install playwright and run visual tests"
          VISUAL_MAX_DIFF=0.03 npm run visual:test || (echo "Visual tests failed; uploading diffs" && exit 1)

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: a11y-artifacts
          path: artifacts/a11y/**
          retention-days: 14

      - name: Comment on PR with summary
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const { owner, repo } = context.repo
            const pr = context.payload.pull_request?.number || context.payload.issue?.number
            const runUrl = `https://github.com/${owner}/${repo}/actions/runs/${context.runId}`
            const body = `A11y automation ran: artifacts are attached to this workflow run. [View workflow run](${runUrl}) — artifacts are uploaded as **a11y-artifacts**. Download and review the screenshots (Playwright), axe JSON, touch-target reports, and Lighthouse HTML where available. If you can't see the artifacts it may be because this PR is from a fork and workflows from forks have restricted permissions; please ask a maintainer to re-run the workflow on the main repo branch or rebase the PR.`;
            try {
              if (pr) {
                await github.rest.issues.createComment({ owner, repo, issue_number: pr, body })
              } else {
                console.warn('No PR number found in event payload; skipping comment.')
              }
            } catch (err) {
              // If commenting fails due to permission restrictions (e.g., PR from fork), surface a debug log with guidance
              console.warn('Failed to post PR comment:', err.message || String(err))
              console.warn('If this is a forked PR, workflows triggered by forks may have read-only tokens. Consider re-running as a maintainer or using `pull_request_target` with careful input validation.')
            }
